/**
 * Re-export PocketBase game sessions hook
 * This maintains backward compatibility with existing imports.
 */
export { useGameSessions } from './useGameSessionsPocketBase';
export type { GameSession, SessionPlayer, CreateSessionInput } from './useGameSessionsPocketBase';
